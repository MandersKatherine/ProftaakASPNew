@model IEnumerable<ASP_PROFTAAK.Models.Reservering>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>
@using (Html.BeginForm("Delete", "Reservering", FormMethod.Post))
{
    

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DatumStart)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DatumEinde)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Betaald)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DatumStart)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DatumEinde)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Betaald)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new {id = item.Id}) |
            @Html.ActionLink("Details", "Details", new {id = item.Id}) |
            @Html.ActionLink("Reservering", "", new {id=item.Id} )

        </td>
    </tr>
}

</table>
}